# Drafts

Pipeline **Stage 4** writes finalized drafts to `drafts/[slug].md` (see `paths.drafts` in `seo-stack-config.yaml`).

Files prefixed with **`example-`** are optional samples you can delete. They are not part of your live site until you run the full pipeline.
